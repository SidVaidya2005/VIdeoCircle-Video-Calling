{
  "name": "videocircle",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Monorepo root — runs pre-commit hooks across the backend/ and frontend/ packages. Each package installs and runs independently.",
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^15.2.10",
    "prettier": "^3.3.3"
  }
}
